version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: cloudkart-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - cloudkart-network

  migration:
    build:
      context: .
      dockerfile: scripts/Dockerfile.migration
    container_name: cloudkart-migration
    env_file:
      - .env.local
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - cloudkart-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cloudkart
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    depends_on:
      migration:
        condition: service_completed_successfully
    networks:
      - cloudkart-network
    restart: unless-stopped

networks:
  cloudkart-network:
    name: cloudkart-network

volumes:
  mongodb_data:
    name: mongodb_data
